# CPU-only torch/torchvision — pulled from the PyTorch CPU index in the
# Dockerfile so ultralytics does NOT drag in the ~2.5 GB CUDA/nvidia wheels.
torch==2.5.1
torchvision==0.20.1

fastapi==0.115.6
uvicorn[standard]==0.34.0
pillow==11.1.0
ultralytics==8.3.55
pyzbar==0.1.9
