# Fallback for theme types this theme doesn't specialise. In practice the
# realm only uses this theme for login (see login/theme.properties, which
# inherits keycloak.v2); this keeps a valid parent for every other type.
parent=keycloak
import=common/keycloak
