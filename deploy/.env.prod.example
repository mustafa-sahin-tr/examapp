# Domain for TLS + public entrypoint (required)
DOMAIN=exam.example.com
LETSENCRYPT_EMAIL=admin@example.com

# Postgres
POSTGRES_USER=examuser
POSTGRES_PASSWORD=CHANGE_ME_STRONG

# Redis
REDIS_PASSWORD=CHANGE_ME_STRONG

# RabbitMQ — RABBITMQ_USER/PASS is the ops-only admin account (management UI
# only, port not exposed publicly here). Issue #279: application services no
# longer share it — the rabbitmq-init job (deploy/scripts/rabbitmq-init.sh)
# creates the 5 per-service users below at deploy time via the management
# HTTP API, with least-privilege permissions (never committed to a tracked
# definitions file, unlike local dev's rabbitmq/definitions.json). Rotate a
# service password by changing its value here and redeploying — rabbitmq-init
# re-runs (idempotent) and updates it.
RABBITMQ_USER=rabbituser
RABBITMQ_PASS=CHANGE_ME_STRONG
RABBITMQ_EXAM_OUTBOX_PASSWORD=CHANGE_ME_STRONG
RABBITMQ_IDENTITY_OUTBOX_PASSWORD=CHANGE_ME_STRONG
RABBITMQ_BADGE_OUTBOX_PASSWORD=CHANGE_ME_STRONG
RABBITMQ_BADGE_SERVICE_PASSWORD=CHANGE_ME_STRONG
RABBITMQ_EXAM_API_PASSWORD=CHANGE_ME_STRONG

# n8n
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CHANGE_ME_STRONG
N8N_EDITOR_BASE_URL=https://exam.example.com/n8n
N8N_WEBHOOK_URL=https://exam.example.com/n8n

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_ME_STRONG

# Keycloak
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=CHANGE_ME_STRONG

# App auth (override appsettings.json values)
JWT_KEY=CHANGE_ME_AT_LEAST_32_CHARS________________________________

# Keycloak clients (set to your realm's values)
KEYCLOAK_CLIENT_ID=exam-client
KEYCLOAK_CLIENT_SECRET=CHANGE_ME
KEYCLOAK_ADMIN_CLIENT_ID=exam-admin
KEYCLOAK_ADMIN_CLIENT_SECRET=CHANGE_ME

# Public base URL (used by services as Authority etc.)
PUBLIC_BASE_URL=https://exam.example.com

# BadgeService AI analyzer toggle (true: analyzer çağrısı aktif, false: kapalı)
BADGE_AI_ACTIVE=true

# (Optional) If you deploy via ACR + GitHub Actions
# ACR_LOGIN_SERVER=myregistry.azurecr.io
# ACR_USERNAME=<acr-username>
# ACR_PASSWORD=<acr-password>
# IMAGE_TAG=<git-sha>
